import { setCanvas, drawFilledCircle, clear, width, height, animate, now } from './graphics.js';
const layerArray = []

class layer {
  constructor(distance, vertices){
    
  }

}

animate((t) => {
  drawlayers()
});
